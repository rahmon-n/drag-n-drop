import React from 'react';

const Home = () => {
  return (
    <div className="mt-4">
      <h1 className="text-2xl font-bold">Now it's a demo version</h1>
      <p>so for now try the editor</p>
    </div>
  );
};

export default Home;
